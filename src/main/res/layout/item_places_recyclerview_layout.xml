<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:foreground="?android:attr/selectableItemBackground"
    android:id="@+id/placeItemLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/placeTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:textSize="@dimen/citiesRVTextSize"
        android:textColor="@color/colorPrimaryDark"
        android:paddingStart="9dp"
        android:paddingEnd="16dp"
        android:paddingTop="@dimen/citiesRM_textView_paddingTop"
        android:paddingBottom="@dimen/citiesRV_textView_paddingBottom"
        android:lines="1"
        android:ellipsize="end"
        tools:text="Item text"
        tools:ignore="UnusedAttribute" />

</FrameLayout>